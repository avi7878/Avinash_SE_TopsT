4) What is Django URLs?make program to create django urls ?

	-- In Django, URLs (Uniform Resource Locators) are used to map specific URL patterns to views, which are Python functions that handle incoming HTTP requests. The URL configuration defines how different parts of your web application are accessed via URLs. This is typically done in a Python module called urls.py within each Django app in your project.